fn f() -> felt252 {
    let a = 2;
    let value0 = 1;

    {% for i in 0..n %}let value{{i + 1}} = value{{i}} * a;
    {% endfor %}

    value{{n}}
}

#[cfg(test)]
mod tests {
    use super::f;

    #[test]
    fn it_works() {
        assert(f() != 0, 'it works!');
    }
}
